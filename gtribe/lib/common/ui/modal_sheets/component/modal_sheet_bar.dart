import 'package:flutter/material.dart';

class ModalSheetBar extends StatelessWidget {
  const ModalSheetBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 4,
      width: 48,
      color: Colors.white.withOpacity(0.2),
    );
  }
}
